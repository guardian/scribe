# Feature Development
* Remove event listeners, emit unbind to plugins so that can too

# Outstanding Patches
* Chrome: Add H1, <enter>, creates `div`

# Known Issues
* Anyone can script an element into the editor. Sanitize only happens on paste. Problem?
